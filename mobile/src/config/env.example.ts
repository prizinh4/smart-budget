// Copie este arquivo para env.local.ts e configure seu IP
// NÃO COMMITE o arquivo env.local.ts

// Para descobrir seu IP, rode no terminal:
// Windows: ipconfig | findstr "IPv4"
// Mac/Linux: ifconfig | grep inet

export const LOCAL_IP = '192.168.0.7'; // Substitua pelo seu IP
